# Таргеты (`target:`)

Здесь опиши подробности по таргетам, если нужно вынести отдельно от таблицы триггеров.

!!! note
    Значение `target:` зависит от конкретного триггера — см. таблицу на странице [Триггеры](triggers.md).
