# Targets (`target:`)

Describe target details here if you need to break them out separately from the triggers table.

!!! note
    The value of `target:` depends on the specific trigger — see the table on the [Triggers](triggers.md) page.
